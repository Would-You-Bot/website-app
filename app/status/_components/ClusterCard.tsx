import { ShardStats } from '../_interfaces'

interface ClusterCardProps {
  data: ShardStats
}

export const ClusterCard = ({ data }: ClusterCardProps) => {
  return <div className="flex p-4"></div>
}
